const SOURCE='https://www.resbank.co.za/en/home/what-we-do/banknotes-and-coin/Use-of-banknotes';
const esc=s=>s.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');
const decode=s=>(s||'').replace(/&amp;/g,'&').replace(/&quot;/g,'"').replace(/&#39;/g,"'").replace(/&lt;/g,'<').replace(/&gt;/g,'>');
async function getHtml(url){const r=await fetch(url,{redirect:'follow',headers:{'User-Agent':'Mozilla/5.0 Notas-do-Mundo/0.6.8','Accept':'text/html,application/xhtml+xml'}});if(!r.ok)throw new Error(`page ${r.status}`);return await r.text();}
async function getImage(url){const r=await fetch(url,{redirect:'follow',headers:{'User-Agent':'Mozilla/5.0 Notas-do-Mundo/0.6.8','Accept':'image/avif,image/webp,image/apng,image/*,*/*;q=0.8'}});if(!r.ok)throw new Error(`image ${r.status}`);const ct=r.headers.get('content-type')||'application/octet-stream';if(!ct.startsWith('image/'))throw new Error(`not image ${ct}`);const bytes=Buffer.from(await r.arrayBuffer());return{statusCode:200,isBase64Encoded:true,headers:{'Content-Type':ct,'Cache-Control':'public,max-age=86400,s-maxage=604800','Access-Control-Allow-Origin':'*'},body:bytes.toString('base64')};}
exports.handler=async event=>{try{const q=event.queryStringParameters||{};if(q.source!=='resbank')return{statusCode:400,body:'unknown source'};const file=q.file||'';if(!/^R(?:10|20|50|100|200)_(?:Front|Back)\.jpg$/i.test(file))return{statusCode:400,body:'bad file'};const html=await getHtml(SOURCE);const re=new RegExp('["\']([^"\']*'+esc(file)+'[^"\']*)["\']','i');const m=html.match(re);if(!m)return{statusCode:404,body:'asset not found in official gallery'};return await getImage(new URL(decode(m[1]),SOURCE).toString());}catch(e){console.error('official-source-image',e);return{statusCode:500,body:'official image fetch failed'};}};
